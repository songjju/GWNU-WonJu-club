# 프로덕션 환경용 환경변수 템플릿
# 실제 값은 GitHub Secrets에서 주입됩니다

# Django 설정 (프로덕션)
SECRET_KEY=production-secret-key-will-be-replaced
DEBUG=False

# 데이터베이스 설정 (프로덕션)
DB_NAME=gwnu_club_db_production
DB_USER=gwnu_user_production
DB_PASSWORD=production-db-password-will-be-replaced
DB_HOST=mysql
DB_PORT=3306

# 이메일 설정 (프로덕션)
EMAIL_HOST_USER=production-email-will-be-replaced
EMAIL_HOST_PASSWORD=production-email-password-will-be-replaced

# MySQL 설정 (프로덕션)
MYSQL_ROOT_PASSWORD=production-mysql-root-password-will-be-replaced