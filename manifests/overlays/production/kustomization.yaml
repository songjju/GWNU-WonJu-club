apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: club-platform-production

namespace: club-platform-production

resources:
  - ../../base
  - namespace.yaml
  - secrets.yaml

patchesStrategicMerge:
  - backend-patch.yaml
  - frontend-patch.yaml

namePrefix: prod-

commonLabels:
  environment: production